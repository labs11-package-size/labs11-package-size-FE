import React from 'react';
import Navigation from '../../components/navigation/Navigation';
import App from '../../App';

const Layout = () => {
	return (
		<div>
			<Navigation />
			<App />
		</div>
	);
};

export default Layout;
